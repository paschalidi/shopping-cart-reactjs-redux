import { expect } from '../test_helper'
import {} from '../../src/actions/types'
import {} from '../../src/actions/index'

describe('actions', () => {
  describe('an action', () => {
    // it('must have correct type', () => {
    // });
    // it('must have correct payload', () => {
    // })
  })
})